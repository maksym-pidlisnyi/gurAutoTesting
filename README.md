# GUR (Glovo-Uber-Raketa sample) Auto-Tests
#### Built with Java + Kotlin + Selenium + TestNG + RestAssured

## Run Setup

```bash
// navigate to suits configurations directory
cd testngxml

// run all the tests
mvn clean test -DsuiteXmlFile=testng.xml
